class SimpleQueue {
    /*
      constructor(elements) {
    this._elements = Array.isArray(elements) ? elements : [];
    this._offset = 0;
  }
  */

    //enqueue (push)
    //dequeue(pop)
    // isEmpty
    // size
    // clear
}