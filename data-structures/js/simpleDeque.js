class SimpleDeque {
    /*
      constructor(elements) {
    this._elements = Array.isArray(elements) ? elements : [];
    this._offset = 0;
  }
  */
  // pushFront
  // pushBack
  // front
  // back
  // popFront
  // popBack
  // isEmpty
  // size
}